/**
 * @file postcss.config.mjs
 * @description Mini README: PostCSS configuration enabling TailwindCSS and autoprefixer for consistent styling across
 * browsers. Tailwind processes component classes while autoprefixer ensures compatibility.
 */

const postcssConfig = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
  }
};

export default postcssConfig;

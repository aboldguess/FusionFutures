# ============================================================================
# File: infra/docker/config/Caddyfile
# Purpose: Configure Caddy reverse proxy with Let's Encrypt support.
# Structure: Routes HTTPS traffic to web and API containers.
# Usage: Mounted into caddy container via docker-compose.
# ============================================================================
{
  email admin@fusionfutures.dev
}

fusionfutures.localhost {
  encode gzip
  reverse_proxy /api/* api:8000
  reverse_proxy web:3100
  tls internal
}
